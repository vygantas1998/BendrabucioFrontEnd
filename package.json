{
  "name": "semestro_projektas",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "graphql": "^14.1.1",
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-relay": "^3.0.0",
    "react-scripts": "2.1.5",
    "relay-runtime": "^3.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build-relay": "relay-compiler --src ./src/ --schema ./graphql/schema.graphql"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "relay-compiler": "^3.0.0"
  }
}
